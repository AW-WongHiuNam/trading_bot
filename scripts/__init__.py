"""Project runnable scripts.

These modules contain the actual implementations.
Root-level files with the same names are thin compatibility stubs so
existing imports/commands (e.g. `python alpha_fetch.py`) keep working.
"""
